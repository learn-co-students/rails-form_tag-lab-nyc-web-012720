<h1>Student Form</h1>

<%= form_for @student do |f| %>
  <%= f.label :first_name %>
  <%= f.text_field :first_name %>

  <%= f.label :last_name %>
  <%= f.text_field :last_name %>

    <%= f.label :submit_student %>
   <%= f.submit %>
   <input type="submit" value="Submit Student">
<% end %>